/* globals exports */
'use strict';

exports.name = '<%= name %>';
exports.up = function(Parse) {
    // up must return a Parse.Promise() that it fulfills when complete.
    console.log('Running migration: ' + exports.name);
    return Parse.Promise.as();
};
